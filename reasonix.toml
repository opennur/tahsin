# ============================================================
# REASONIX CONFIG — ALLOW EVERYTHING + PYTHON SCRIPTS
# ============================================================

[sandbox]
bash = "off"
network = true

[permissions]
allow = [
    "Bash=.*",  # ALL Bash commands (including Python scripts)
    "review",
    "apply",
    "status",
    "run",
]